fastapi>=0.110.0
uvicorn[standard]>=0.28.0
requests>=2.31.0
httpx[http2]>=0.27.0
pydantic>=2.0.0
bcrypt>=4.1.0
pyjwt>=2.8.0
pyyaml>=6.0.1
reportlab>=4.0.0
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.3.0
# ── New in v2.1 (Enterprise Upgrade) ──────────────────────────────────────────
# Redis client — leaky bucket rate limiter + L2 threat intel cache
redis>=5.0.0
# Aho-Corasick C extension — O(n) multi-keyword pre-filter (10x faster than regex-only)
# Falls back gracefully to pure-Python substring scan if unavailable
pyahocorasick>=2.0.0
# Async file I/O — used by nginx log agent and cert generation
aiofiles>=23.0.0
# PostgreSQL client — required when DATABASE_URL=postgresql://...
psycopg2-binary>=2.9.0
